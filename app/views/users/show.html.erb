<%= provide(:title, @user.team) %>

<div class="row">
	<div class="col-sm-8 col-sm-offset-2 center">
		<h1><%= @user.team %></h1>
		<h4>Managed by: <%= @user.name %></h4>
	</div>
</div>

<div class="row center">
	<div class="col-sm-7">
		<h3>MLB Roster</h3>
		<table class="table table-bordered">
			<% @players.where(level_id: 1).order(:position_id).each do |player| %>
				<tr>
					<td><%= player.position.spot %></td>
					<td><%= "#{player.first_name} #{player.last_name}" %></td>
					<% if (logged_in? && @user.id == current_user.id) || (logged_in? && current_user.admin?) %>
						<td><%= link_to "Demote or DL", edit_player_path(player) %></td>
						<td><%= link_to "Drop", drop_player_path(player) %></td>
					<% end %>
				</tr>
			<% end %>
		</table>
	</div>
	<div class="col-sm-5">
		<h3>60 Day DL</h3>
		<table class="table table-bordered">
			<% @players.where(level_id: 2).order(:position_id).each do |player| %>
				<tr>
					<td><%= player.position.spot %></td>
					<td><%= "#{player.first_name} #{player.last_name}" %></td>
					<% if (logged_in? && @user.id == current_user.id) || (logged_in? && current_user.admin?) %>
						<td><%= link_to "Demote or DL", edit_player_path(player) %></td>
					<% end %>
				</tr>
			<% end %>
		</table>
	</div>
</div>

<div class="row center">
	<div class="col-sm-4">
		<h3>A Team</h3>
		<table class="table table-bordered">
			<% @players.where(level_id: 3).order(:position_id).each do |player| %>
				<tr>
					<td><%= player.position.spot %></td>
					<td><%= "#{player.first_name} #{player.last_name}" %></td>
					<% if (logged_in? && @user.id == current_user.id) || (logged_in? && current_user.admin?) %>
						<td><%= link_to "Promote", edit_player_path(player) %></td>
						<td><%= link_to "Drop", drop_player_path(player) %></td>
					<% end %>
				</tr>
			<% end %>
		</table>
	</div>
	<div class="col-sm-4">
		<h3>AA Team</h3>
		<table class="table table-bordered">
			<% @players.where(level_id: 4).order(:position_id).each do |player| %>
				<tr>
					<td><%= player.position.spot %></td>
					<td><%= "#{player.first_name} #{player.last_name}" %></td>
					<% if (logged_in? && @user.id == current_user.id) || (logged_in? && current_user.admin?) %>
						<td><%= link_to "Promote", edit_player_path(player) %></td>
						<td><%= link_to "Drop", drop_player_path(player) %></td>
					<% end %>
				</tr>
			<% end %>
		</table>
	</div>
	<div class="col-sm-4">
		<h3>AAA Team</h3>
		<table class="table table-bordered">
			<% @players.where(level_id: 5).order(:position_id).each do |player| %>
				<tr>
					<td><%= player.position.spot %></td>
					<td><%= "#{player.first_name} #{player.last_name}" %></td>
					<% if (logged_in? && @user.id == current_user.id) || (logged_in? && current_user.admin?) %>
						<td><%= link_to "Promote", edit_player_path(player) %></td>
						<td><%= link_to "Drop", drop_player_path(player) %></td>
					<% end %>
				</tr>
			<% end %>
		</table>
	</div>
</div>

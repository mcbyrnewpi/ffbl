<% provide(:title, "Commissioner's Page") %>

<div class="container">
  <div class="row content-container">
    <div class="col-md-8 col-md-offset-2">
      <%= simple_form_for @commish_note do |f| %>
        <%= f.input :commish_note_content, label: "Edit Commish Note", :required => false %>
        <%= f.button :submit, class: "btn btn-cancel" %>
      <% end %>
    </div>
  </div>
</div>
<%= simple_form_for @player do |f| %>
	<%= f.input :user_id, :as => :hidden, :input_html => { :value => nil } %>
	<%= f.input :level_id, :as => :hidden, :input_html => { :value => nil } %>
	<%= f.button :submit, "Drop #{@player.first_name} #{@player.last_name}?", class: "btn btn-danger" %>
<% end %>
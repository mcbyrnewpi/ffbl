<%= provide(:title, "Drop #{@player.first_name} #{@player.last_name}") %>
<div class="container content-container">
<div class="row">
	<% if (@player.user_id == current_user.id) || current_user.admin? %>
		<div class="col-sm-12 center give-me-top-padding">
			<h1>Choose His Destiny...</h1>
		</div>
		<div class="col-sm-12">
			<%= render 'drop_player_form' %>
		</div>		
	<% else %>
			<h1>Player is owned by <%= @player.user.team %>... trade for him or PISS OFF!!</h1>
	<% end %>
</div>
</div>
<%= provide(:title, "View Transaction") %>
<div class="container content-container">
  <div class="row">

  	<% if !@transaction.details.blank? %>
	    <div class="col-sm-10 col-sm-offset-1 center">
				<h1>Trade involving:</h1>
				<h3><%= @transaction.player.first_name %> <%= @transaction.player.last_name %></h3>
			</div>
			<div class="col-sm-6 col-sm-offset-3 center give-me-top-margin">
				<h3>Full Trade Details:</h3>
				<p><%= simple_format(@transaction.details) %></p>
			</div>
		<% else %>
			<div class="col-sm-12 center">
				<h1 class="space-my-bottom">Why do you want to look at this transaction?</h1>
				<p>The details were already displayed, weirdo...</p>
			</div>
		<% end %>
	</div>
</div>
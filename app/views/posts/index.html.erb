<%= provide(:title, "All Posts") %>

<div class= "container content-container">
	<div class="row">
		<div class="col-sm-12">
			<h1 class="space-my-bottom center">FFBL Message Board</h1>
		</div>
		<div class="col-sm-4">
				<%= link_to "Create a New Topic", new_post_path, class: "btn btn-cancel" %>
		</div>
		<div class="col-sm-12">
			<table class="table">
				<tr>
					<th>Created</th>
					<th>By</th>
					<th>Topic</th>
					<th>Replies</th>
					<th>Most Recent</th>
				</tr>
				<% @posts.each do |post| %>
					<tr>
						<td><small><%= post.created_at.strftime("%b %-d, %Y") %> at <%= post.created_at.strftime("%l:%M %P") %></small></td>
						<td><small><%= link_to post.user.team, user_path(post.user), class: "team-link" %></small></td>
						<td><%= link_to post.topic, post_path(post), class: "post-link" %></td>
						<td><small><%= post.responses.count %> replies</small></td>
						<% if post.responses.count > 0 %>
							<td><small><%= time_ago_in_words(post.responses.last.created_at) %> ago</small></td>
						<% else %>
							<td></td>
						<% end %>
					</tr>
				<% end %>
			</table>
		</div>
	</div>
</div>
